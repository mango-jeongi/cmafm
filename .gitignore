# Python cache
__pycache__/
*.pyc
*.pyo
*.pyd
.ipynb_checkpoints/

# Python Virtual Environments
.venv/
env/
venv/
ENV/

# VSCode / IDE settings
.vscode/
.idea/

# Build artifacts
build/
dist/
*.egg-info/

# System files
.DS_Store
Thumbs.db
desktop.ini

# CFT engine (cloned by setup.sh, not committed)
CFT_repo/
cft_engine/

# Model weights (too large to commit; download separately per README)
weights/
*.pt
*.pth
*.ckpt
*.onnx
*.engine

# Local path configuration (machine-specific, do not commit)
scripts/apply_local_paths.py

# Local Environment Variables (machine-specific secrets)
.env

# HPC outputs, TensorBoard logs, and training run artifacts
logs/
runs/*

# Bundled FLIR driving sequence demos for the video tracking dashboard tab
!runs/flir_v1_rgb.mp4
!runs/flir_v1_thermal.mp4

# Supplementary zip artifacts (built by scripts/build_supplementary.sh)
*.zip
CMAFM_Supplementary_*.zip

# SSH tunnel logs
ssh_tunnel_watchdog.log
